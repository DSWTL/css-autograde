# CSS-Lint-HW

## Assignment
Our CSS linter is failing. Read the test output and update the style.css file to make the tests pass.

### Set up  
`npm install`

### Run test  
`npx stylelint "**/*.css"`
